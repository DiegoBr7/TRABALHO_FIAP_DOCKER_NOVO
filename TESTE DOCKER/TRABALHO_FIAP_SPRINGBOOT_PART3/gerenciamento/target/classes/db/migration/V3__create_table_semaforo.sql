-- Cria a tabela TB_SEMAFORO com ID auto-incrementado
CREATE TABLE TB_SEMAFORO (
    ID INT NOT NULL AUTO_INCREMENT,
    LOCALIZACAO VARCHAR(255) NOT NULL,
    STATUS VARCHAR(50) NOT NULL,
    FLUXO_TRAFEGO INT NOT NULL,
    PRIMARY KEY (ID)
);
